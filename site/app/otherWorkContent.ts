/**
 * Deprecated 2026-08-05: merged into site/app/archive/archiveContent.ts.
 *
 * The "Other work" tier and the archive tier read as the same thing to a
 * visitor (real work, no full case study), so they're one system now, one
 * grid, one page per project under /archive/[slug]. Bloomberg, Ford, and
 * Sour Patch Kids moved there; Nike was promoted to a full case study at
 * /nike. Nothing in the app imports this file any more — kept only so the
 * history of the decision isn't lost. Safe to delete.
 */
export {};
