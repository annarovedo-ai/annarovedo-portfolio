"use client";

/**
 * UNUSED — safe to delete. The hero chat stage lived one day (2026-08-18 to
 * 2026-08-19) before Anna's own question ("do you think the chat belongs in
 * the hero? at all?") retired it: a hero's job is identity and credibility
 * in five seconds, and chat asks for effort before showing proof. The chat
 * surface lives on in AlmostAnnaChat.tsx inside the razor's panel; the
 * stageDocked plumbing in chatStore.ts is dormant. This file is a stub kept
 * only because the sandbox cannot delete files; nothing imports it.
 */
export default function AnnaStage() {
  return null;
}
