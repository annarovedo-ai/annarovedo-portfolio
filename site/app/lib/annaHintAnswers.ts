/**
 * UNUSED — safe to delete. The shared hint bank moved into annaAnswers.ts
 * on 2026-08-19 (the node test runner loads that module directly and cannot
 * resolve extensionless value imports between .ts files). Kept only because
 * this sandbox cannot delete files. Nothing imports this.
 */
export {};
